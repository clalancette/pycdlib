Summary: Pure python ISO9660 read and write library
Name: python-pyiso
Version: @VERSION@
Release: @RELEASE@%{?dist}
License: LGPLv2
Group: Development/Libraries
URL: http://github.com/clalancette/pyiso
Source0: http://github.com/clalancette/pyiso/archive/pyiso-%{version}.tar.gz
BuildArch: noarch
Requires: python >= 2.5
Requires: python-sendfile

BuildRequires: python

%description
Pyiso is a pure python library for reading, writing, and otherwise manipulating
ISO9660 files.  It is focussed on speed, correctness, and conformance to the
various standards around ISO9660, including ISO9660 itself, the Joliet
extensions, the Rock Ridge extensions, and the El Torito boot extensions.

%prep
%setup -q -n pyiso-%{version}

%build
python setup.py build

%install
python setup.py install --root=$RPM_BUILD_ROOT --skip-build

%files
%doc COPYING
%{python_sitelib}/pyiso/*
%{python_sitelib}/pyiso-*.egg-info
%{_bindir}/pyiso-explorer
%{_bindir}/pyiso-compare

%changelog

