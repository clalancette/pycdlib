1.  We can't currently generate Rock Ridge CL (Child Link), PL (Parent Link),
    and RE (relocated directory) entries when we need them.
2.  Rock Ridge SF (Sparse file) entries don't work for parsing or generation.
3.  Large files (up to 4GB-1) don't work, but should.
4.  We need to add tests for Joliet + Eltorito + RockRidge.
5.  Need to support ISO9660:1999 (ISO9660 version 2, ISO level 4).
6.  Support for UDF.
7.  PyDoc documentation.
8.  Examples/usage documentation.
9.  More tests.
10. Make pyiso-explorer deal with Joliet properly.
11. Example programs.
12. High-level documentation.
13. RPM package.
14. Debian package.
15. PyPI?
16. Potentially optimize reshuffle_extents() by rewriting in Cython.
17. Implement EFI for the isohybrid support.
18. Figure out a better way to deal with file object lifetimes.
