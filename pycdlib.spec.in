Summary: Pure python ISO9660 read and write library
Name: python-pycdlib
Version: @VERSION@
Release: @RELEASE@%{?dist}
License: LGPLv2
Group: Development/Libraries
URL: http://github.com/clalancette/pycdlib
Source0: http://github.com/clalancette/pycdlib/archive/pycdlib-%{version}.tar.gz
BuildArch: noarch
Requires: python >= 2.5
Requires: pysendfile

BuildRequires: python

%description
Pycdlib is a pure python library for reading, writing, and otherwise
manipulating ISO9660 files.  It is focussed on speed, correctness, and
conformance to the various standards around ISO9660, including ISO9660 itself,
the Joliet extensions, the Rock Ridge extensions, and the El Torito boot
extensions.

%package -n pycdlib-tools
Summary: Tools that rely on pycdlib.
Requires: python-pycdlib
Requires: python-networkx
Requires: python-matplotlib

%description -n pycdlib-tools
Some tools that use the pycdlib library.

%prep
%setup -q -n pycdlib-%{version}

%build
python setup.py build

%install
python setup.py install --root=$RPM_BUILD_ROOT --skip-build

%files
%doc COPYING examples/*
%{python_sitelib}/pycdlib/*
%{python_sitelib}/pycdlib-*.egg-info

%files -n pycdlib-tools
%{_bindir}/pycdlib-explorer
%{_bindir}/pycdlib-compare

%changelog
* Wed Dec 30 2015 Chris Lalancette <clalancette@gmail.com> - 0.1.0-1
- Initial package.
